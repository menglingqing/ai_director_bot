# S1：剧本 → 分镜表/镜头列表

**阶段**：从剧本到可执行镜头  
**状态**：🆕 建议新增

## 输入

- 剧本（来自 S0 的 `script`）

## 输出

- 分镜表（结构化）：镜号、景别、内容摘要、对白、时长、备注等，便于按镜调度后续技能

## 在流水线中的位置

- 紧接 S0，产出 `shotlist`，供 S2（分镜概念、角色、场景）使用。

## 待实现

- [ ] 定义分镜表数据结构（如 JSON Schema 或 YAML 示例）
- [ ] 编写 SKILL.md：触发条件、输入输出格式、示例
- [ ] 提示词模板：如何从剧本生成结构化分镜表（可交给 LLM 生成后再解析）
