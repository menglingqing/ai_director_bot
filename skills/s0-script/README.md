# S0：根据创意进行剧本创作的提示词生成

**阶段**：创意与剧本  
**状态**：✅ 已有（实现可放在 ai_director_bot 或外部）

## 输入

- 用户创意 / 梗概 / 方向（自然语言）

## 输出

- 结构化剧本：场次、场景、人物、对白、动作描述等（生成提示词或直接剧本文本）

## 在流水线中的位置

- 入口阶段，产出 `script`，供 S1 分镜表使用。

## 实现说明

- 若已有现成剧本生成能力，本目录可仅保留本 README，并在 `workflow/default.yaml` 中保持 `s0-script` 的引用。
- 若在本仓实现，可在此目录添加 `SKILL.md` 与 `references/` 模板。
